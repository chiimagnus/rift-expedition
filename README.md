# 双生圣痕 · 血脉宿命战棋

Web 像素风战棋，基于 Phaser 4 + TypeScript。

## 本地冒烟测试

冒烟测试需要 `vendor/phaser.min.js`（Phaser 4.2.0，与 CDN 同版本）。该文件体积大、**不纳入 git**，请从 Notion《双生圣痕 · 源代码存档》页的 `phaser.js.zip` 取回后放到 `vendor/`，再运行 `bash scripts/gate.sh`。
