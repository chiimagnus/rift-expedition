name: RiftExpedition
options:
  minimumXcodeGenVersion: 2.44.0
  createIntermediateGroups: true
settings:
  base:
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
packages:
  RiftCore:
    path: Packages/RiftCore
targets:
  SKTiled:
    type: framework
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: Vendor/SKTiled/Sources
        excludes:
          - Info.plist
          - SKTiled.h
    settings:
      base:
        PRODUCT_NAME: SKTiled
        GENERATE_INFOPLIST_FILE: true
        SWIFT_VERSION: "5.5"
        SWIFT_STRICT_CONCURRENCY: minimal
  RiftExpedition:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: RiftExpedition
        excludes:
          - Resources
      - path: RiftExpedition/Resources/Assets
        buildPhase: resources
        type: folder
      - path: RiftExpedition/Resources/Data
        buildPhase: resources
        type: folder
      - path: RiftExpedition/Resources/Maps
        buildPhase: resources
        type: folder
    dependencies:
      - package: RiftCore
      - target: SKTiled
    entitlements:
      path: RiftExpedition/RiftExpedition.entitlements
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.riftexpedition.game
        PRODUCT_NAME: RiftExpedition
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_KEY_CFBundleDisplayName: 裂隙远征
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.games
        CODE_SIGN_STYLE: Automatic
  RiftExpeditionTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: RiftExpeditionTests
    dependencies:
      - target: RiftExpedition
    settings:
      base:
        GENERATE_INFOPLIST_FILE: true
schemes:
  RiftExpedition:
    shared: true
    build:
      targets:
        RiftExpedition: all
    test:
      gatherCoverageData: false
      targets:
        - name: RiftExpeditionTests
